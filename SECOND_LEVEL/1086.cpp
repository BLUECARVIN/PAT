#include <cstdio>
#include <iostream>
#include <cstring>
#include <stack>
#include <algorithm>

using namespace std;

struct Node
{
    int val;
    Node* lchild;
    Node* rchild;
};

int main()
{
    stack<int> s;
    int n;
    scanf("%d", &n);
    char con[5];
    for(int i = 0; i < 2*n; i++)
    {
        scanf("%s", &con);
        if(strcmp(con))        
    }
}